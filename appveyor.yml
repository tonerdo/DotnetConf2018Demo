version: '{build}'
image:
  - Visual Studio 2015
  - Ubuntu
configuration:
  - Debug
  - Release
build_script:
  - ps: echo "Building for $env:CONFIGURATION"
  - ps: dotnet build
test_script:
  - ps: dotnet test /p:CollectCoverage=true
